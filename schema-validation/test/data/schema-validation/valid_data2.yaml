shared_bucket: "boette"
source_folder_prefix: "led-still/delt/"
destination_folder: "led-still/delt"
memory_size: 2
pseudo:
  - name: task_one
    columns: ["fornavn", "etternavn"]
    encryption:
      method: "with_default_encryption"
  - name: task_two
    columns: ["fnr"]
    encryption:
      method: "with_stable_id"
      key: "papis-common-key-1"
